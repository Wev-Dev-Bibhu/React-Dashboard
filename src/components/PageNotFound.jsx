import { Box } from "@mui/material";
import React from "react";

const PageNotFound = () => {
  return (
    <Box
      height={200}
      width="100%"
      sx={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontSize: 20,
      }}
    >
      Feature Coming Soon !!
    </Box>
  );
};

export default PageNotFound;
